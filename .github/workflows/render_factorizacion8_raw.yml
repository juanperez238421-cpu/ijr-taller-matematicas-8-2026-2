name: Render Factorizacion 8 Raw Manim (SUSPENDED)

on:
  workflow_dispatch:

jobs:
  suspended:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "IJR Manim render suspended on 2026-08-04 to prevent GitHub Actions usage."
